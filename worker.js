/*
const Data = await ClubTracking.find();

        Promise.all(Data.map(async(ClubData) => {
            if(ClubData.isNewClub == true){
                const ClubResponse = await getClub(ClubData.clubTag);
                if(ClubResponse == "No Data Found") return;
                
                
            };
}));
*/